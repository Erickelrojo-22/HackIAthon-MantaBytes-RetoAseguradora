# Copy these values into Streamlit Cloud secrets, not into a committed secrets.toml.
OPENAI_API_KEY = "paste_the_team_key_in_the_platform_secret"
OPENAI_MODEL = "gpt-5.4-mini"

FRAUDIA_DB_BACKEND = "sqlite"
FRAUDIA_DB_PATH = "data/processed/fraudia_claims.db"
FRAUDIA_DATA_SOURCE = "demo"
